# Optional: backend origin for production builds.
# In dev, leave unset and Vite will proxy /api/* to http://localhost:8000.
# In production, point this to the backend URL, e.g.:
# VITE_API_URL=https://api.solara-optima.example.com
VITE_API_URL=
